openapi: 3.2.0
info:
  version: '2.16'
  title: Penpot RPC Get Nitrate Connectivity API
  description: Penpot RPC-style REST API for projects, files, pages, components, and design assets.
  x-api-id: penpot-rpc
  contact:
    url: https://community.penpot.app/
    name: Penpot Support
  x-humanURL: https://penpot.app/integrations-api
servers:
- url: https://design.penpot.app/api/main/methods
  description: MAIN API
tags:
- name: Get Nitrate Connectivity
paths:
  get-nitrate-connectivity:
    post:
      description: null
      deprecated: false
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties: {}
            example: '{}'
      responses:
        default:
          description: A default response
          content:
            application/json:
              schema:
                title: nitrate-connectivity
                type: object
                properties:
                  licenses:
                    $ref: '#/components/schemas/Boolean'
                required:
                - licenses
              example: '{"licenses":false}'
      tags:
      - Get Nitrate Connectivity
      summary: Create get nitrate connectivity
      x-summary-source: derived
      operationId: postGetNitrateConnectivity
      x-operation-id-source: derived
components:
  schemas:
    Boolean:
      title: boolean
      description: boolean
      type: boolean
