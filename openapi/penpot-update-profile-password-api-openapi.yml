openapi: 3.0.0
info:
  version: '2.16'
  title: Penpot RPC Add Team To Organization Update Profile Password API
  description: Penpot RPC-style REST API for projects, files, pages, components, and design assets.
  x-api-id: penpot-rpc
  contact:
    url: https://community.penpot.app/
    name: Penpot Support
  x-humanURL: https://penpot.app/integrations-api
servers:
- url: https://design.penpot.app/api/main/methods
  description: MAIN API
tags:
- name: Update Profile Password
paths:
  update-profile-password:
    post:
      description: null
      deprecated: false
      requestBody:
        required: true
        content:
          application/json:
            schema:
              title: update-profile-password
              type: object
              properties:
                password:
                  title: string
                  description: string
                  type: string
                  format: string
                oldPassword:
                  title: string
                  description: string
                  type: string
                  format: string
                  nullable: true
              required:
              - password
            example: '{"password":"krua","oldPassword":"surswytc"}'
      tags:
      - Update Profile Password
