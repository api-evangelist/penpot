openapi: 3.2.0
info:
  version: '2.16'
  title: Penpot RPC Update Profile Password API
  description: Penpot RPC-style REST API for projects, files, pages, components, and design assets.
  x-api-id: penpot-rpc
  contact:
    url: https://community.penpot.app/
    name: Penpot Support
  x-humanURL: https://penpot.app/integrations-api
servers:
- url: https://design.penpot.app/api/main/methods
  description: MAIN API
tags:
- name: Update Profile Password
paths:
  update-profile-password:
    post:
      description: null
      deprecated: false
      requestBody:
        required: true
        content:
          application/json:
            schema:
              title: update-profile-password
              type: object
              properties:
                password:
                  title: string
                  description: string
                  type: string
                  format: string
                oldPassword:
                  title: string
                  description: string
                  type:
                  - string
                  - 'null'
                  format: string
              required:
              - password
            example: '{"password":"krua","oldPassword":"surswytc"}'
      tags:
      - Update Profile Password
      summary: Create update profile password
      x-summary-source: derived
      operationId: postUpdateProfilePassword
      x-operation-id-source: derived
