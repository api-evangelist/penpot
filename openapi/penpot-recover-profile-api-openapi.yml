openapi: 3.2.0
info:
  version: '2.16'
  title: Penpot RPC Recover Profile API
  description: Penpot RPC-style REST API for projects, files, pages, components, and design assets.
  x-api-id: penpot-rpc
  contact:
    url: https://community.penpot.app/
    name: Penpot Support
  x-humanURL: https://penpot.app/integrations-api
servers:
- url: https://design.penpot.app/api/main/methods
  description: MAIN API
tags:
- name: Recover Profile
paths:
  recover-profile:
    post:
      description: null
      deprecated: false
      requestBody:
        required: true
        content:
          application/json:
            schema:
              title: recover-profile
              type: object
              properties:
                token:
                  title: string
                  description: string
                  type: string
                  format: string
                password:
                  title: string
                  description: string
                  type: string
                  format: string
              required:
              - token
              - password
            example: '{"token":"pfzeb","password":"neyi"}'
      tags:
      - Recover Profile
      summary: Create recover profile
      x-summary-source: derived
      operationId: postRecoverProfile
      x-operation-id-source: derived
