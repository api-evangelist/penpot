openapi: 3.0.0
info:
  version: '2.16'
  title: Penpot RPC Add Team To Organization Logout API
  description: Penpot RPC-style REST API for projects, files, pages, components, and design assets.
  x-api-id: penpot-rpc
  contact:
    url: https://community.penpot.app/
    name: Penpot Support
  x-humanURL: https://penpot.app/integrations-api
servers:
- url: https://design.penpot.app/api/main/methods
  description: MAIN API
tags:
- name: Logout
paths:
  logout:
    post:
      description: Clears the authentication cookie and logout the current session.
      deprecated: false
      requestBody:
        required: true
        content:
          application/json:
            schema:
              title: logoug
              type: object
              properties:
                profileId:
                  $ref: '#/components/schemas/Uuid'
            example: '{}'
      tags:
      - Logout
components:
  schemas:
    Uuid:
      title: uuid
      description: UUID formatted string
      type: string
      format: uuid
